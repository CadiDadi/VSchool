// Node
    function addTogether(num1, num2){
        return num1 + num2
    }

    console.log(addTogether(2, 3))

//  To run a file:   node fileName.js

// Javascript Best Practices & Conventions

    // camelCase
    var firstName = "joe"
    function iAmAFunction(){

    }

    // white space
    var firstName = 'nate'

    // Indenting (children should be indented from their parents)
    function addTogether(num1, num2){
        return num1 + num2
    }

    // Braces
    function timesTogether(num1, num2){

    }

    var myObject = {

    }

    // Naming ( Be nice to yourself and call it what it is )
    var book = {}
    var blanket = {}

    function addTogether(num1, num2){
        return num1 + num2
    }

        // variable to track whether or not user is alive
        var isAlive = true

        // Arrays
        ['strings', 'str2', 'str3']






// Debugging

